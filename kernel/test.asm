; This is temporary file, I use it to test syscalls, it's just easier.
; It will be helpfull in shell development!

;GLOBAL interruptTest

;interruptTest:
    ;mov eax, 0x2
    ;mov edx, text_string
    ;int 0x80
    ;ret

    ;text_string db 'Hello!', 0